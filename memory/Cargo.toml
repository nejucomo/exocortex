[package]
edition.workspace = true
name = "exocortex-memory"
version = "0.1.0"

[dependencies]
derive-new.workspace = true
derive_more = { workspace = true, features = ["from", "into", "try_into"] }
exocortex-lid.workspace = true
