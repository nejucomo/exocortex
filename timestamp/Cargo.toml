[package]
edition.workspace = true
name = "exocortex-timestamp"
version = "0.1.0"

[dependencies]
derive_more = { workspace = true, features = ["deref"] }
jiff.workspace = true
redb = { workspace = true, optional = true }
